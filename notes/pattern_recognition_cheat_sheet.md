Duplicate / seen before       → Set / HashMap
Anagram / frequency           → HashMap counting
Target sum                    → Complement lookup
Sorted array + search         → Binary Search
Compare both ends             → Two Pointers
Remove/move/modify in-place   → Two pointers / overwrite
Maximum/best so far           → Running sum / tracking
Before and after each index   → Prefix / Suffix